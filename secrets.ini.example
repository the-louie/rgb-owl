; Copy to secrets.ini (gitignored) and fill in. CI writes it from repository secrets.
[owl_secrets]
; ArduinoOTA, web .bin upload, /api/devmode
ota_password = change-me
; BLE pairing passkey, 6 digits
ble_pin = 123456
